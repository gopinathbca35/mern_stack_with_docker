apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv
  namespace: mern-app
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/mongodb
  persistentVolumeReclaimPolicy: Retain     